{
  "name": "backend-epouvante",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --detectOpenHandles --runInBand",
    "lint": "node --check src/server.js && node --check src/database.js",
    "audit": "npm audit --audit-level=high",
    "test:load": "node test-charge.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^4.22.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.1",
    "sqlite3": "^5.1.7"
  },
  "overrides": {
    "tar": "^7.5.8"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  }
}
